import Button from './Button';
import { Meta, Story } from '@storybook/addon-docs';
import { MemoryRouter } from 'react-router-dom';

<Meta title="Components/Button" component={Button} />

export const Template = (args) => (
  <MemoryRouter>
    <Button {...args} />
  </MemoryRouter>
);

# Button component

The Button represents the Add Sample Button, Add Track Button and Add Artist Button

<Story
  name="Add Sample Button"
  args={{
    backgroundColor: '#F7C4D4',
    children: 'Add Sample',
    color: '#333333',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Add Track Button"
  args={{
    backgroundColor: '#00CE82',
    children: 'Add Track',
    color: '#333333',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Add Artist Button"
  args={{
    backgroundColor: '#5B81EE',
    children: 'Add Artist',
    color: '#333333',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Long Button Text"
  args={{
    backgroundColor: '#F7C4D4',
    children: 'Yeah, space is the place',
    color: '#333333',
  }}
>
  {Template.bind({})}
</Story>
